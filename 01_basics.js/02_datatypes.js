"use strict";
// on writing this the old standard codes are seen as new ;
// we treat all the system code on newer version;
// this is applied over whole page once applied
// alert('hello sweta');
//we are using javascript file which is not embedded in html;
// code reeadabality is priority
// standards given by ecma;
let name="sweta";
let age=18
let isloggedIn =false;
// string =" ";
// number=. 2 to the power 53
// bigint 
// null (standalone value)
//bollean(t/f)
// symbol => unique
// undefined;
console.log(typeof undefined );//undefine.
 console.log(typeof null );  //it is object;
 